pipeline {
  agent any
  stages {
    stage('create xml file') {
      steps {
            sh 'echo "Inside my first stage"'
            sh 'echo "Lets create an xml file"'
            sh '''pwd
            ls
            ls -lart'''
      }
    }
  }
}
