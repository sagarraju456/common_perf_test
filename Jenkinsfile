pipeline {
  agent none
  stages {
    stage('create xml file') {
      steps {
        script{
          echo 'Inside my first stage'
          echo 'Lets create an xml file'
          sh '''pwd
          ls
          ls -lart'''
        }
      }
    }
  }
}
